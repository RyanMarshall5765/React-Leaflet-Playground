import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import {pointToLayer} from '../components/Map'

it('works', () => {
  expect(1).toBe(1)
})


it('Basillian Marker', () => 
expect(pointToLayer({
  
})).toBe())
